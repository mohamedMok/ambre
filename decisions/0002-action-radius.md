# Action radius

`radius.md` is the control corner: fields, disclosure, and quiet surfaces.

`radius.action` is the button corner. It is a public token because a brand must be able to make actions pills without changing fields. In the Ambre theme it is an alias of `radius.md`, so the default look does not change.

A brand sets the value in its preset file. Components do not read a second source.
