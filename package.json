{
  "name": "ambre",
  "private": true,
  "version": "0.0.0",
  "description": "Open-source design system. One library of Svelte web components.",
  "type": "module",
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @ambre-ds/tokens build && pnpm --filter @ambre-ds/ui build && pnpm --filter @ambre-ds/commerce build && pnpm --filter @ambre-ds/ai build",
    "test": "pnpm --filter @ambre-ds/tokens test && pnpm --filter @ambre-ds/ui test",
    "docs": "pnpm --filter @ambre-ds/docs dev",
    "storybook": "pnpm --filter @ambre-ds/storybook dev",
    "check": "pnpm -r --if-present check",
    "social": "node scripts/social/render.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version && pnpm install --lockfile-only",
    "release": "pnpm build && pnpm test && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "ajv": "^8.20.0",
    "yaml": "^2.9.1"
  },
  "license": "MIT"
}
